export enum InputType {
  INPUT = 'input',
  PASSWORD = 'password',
  SELECT = 'select',
}

export enum AuthenticationType {
  USERANDPASSWORD = '1',
  NONE = '2',
}

export enum SSHAuthenticationType {
  PASSWORD = 1,
  KEYPAIR = 2,
  OPENSSH = 3
}
